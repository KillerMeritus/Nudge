import styles from './Timer.module.css';

export default function Timer() {
  return (
    <div className={styles.container}>
      <p className={styles.placeholder}>⏱ Timer — coming soon</p>
    </div>
  );
}
